platform :ios, '16.0'
use_frameworks!

target 'XsiteDubai' do
  # ✅ Clean, lightweight libraries
  pod 'Alamofire'      # For API calls to xsite.ae or Property Finder
  pod 'SDWebImage'     # For loading property images fast
end
